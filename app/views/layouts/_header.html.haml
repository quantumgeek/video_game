#header
	.container
		.span-12.left
			%h1.site_name 
				= link_to "Game Shelf", root_path
		#top_nav.span-11.right
			- if current_user
				= link_to "Profile", user_path(current_user)
				= link_to "Log out", logout_path
			- else
				= link_to "Sign Up", sign_up_path
				= link_to "Log In", login_path
#site-nav